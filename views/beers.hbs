<section class='beers'>
  <ul>
    {{#each beers}}
      <div><img src={{this.image_url}} width="100" height="300" />
        <li><h3> {{this.name}} </h3>
        <p> {{this.description}} </p>
        <p> {{this.tagline}} </p>
      </li>
      </div>

    {{/each}}
  </ul>
</section>